apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  # ClusterIP for internal database access only
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
  # Headless service for StatefulSet DNS
  clusterIP: None
