apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
data:
  # Non-sensitive PostgreSQL configuration
  # Separating config from secrets is a Kubernetes best practice
  POSTGRES_HOST: postgres
  POSTGRES_PORT: "5432"
  POSTGRES_DB: toyapi
  POSTGRES_USER: postgres
