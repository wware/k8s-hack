apiVersion: v1
kind: Secret
metadata:
  name: api-key-secret
type: Opaque
stringData:
  # In production, generate this with a real random-token tool and rotate
  # it periodically -- for this learning exercise, a fixed value keeps the
  # curl examples in the book reproducible.
  API_KEY: demo-key-a1b2c3d4e5
